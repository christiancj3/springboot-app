pipeline {
	agent any
	stages {
		stage('Build') {
			steps {
				echo "Etape de build"
				//sh 'python --version'
			}
		}
		stage('Tests') {
			steps { 
				echo "Etape de test"
				 sh 'whoami'
                                 sh './mvnw test'
			}
		}
		stage ('Deploy') {
			steps {
				echo "Etape de déploiement"
			}
		}
	}
}
